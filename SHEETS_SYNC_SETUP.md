# 운동일지 구글시트 동기화 설정

앱(GitHub Pages)은 정적 페이지라 구글시트에 직접 쓸 수 없고, 시트에 붙은 **Apps Script 웹앱**이 중간에서 읽고 씁니다.
한 번만 설정하면 이후엔 앱에서 저장할 때마다 자동으로 시트에 올라갑니다.

## 1. 시트 만들기
1. 구글 드라이브에서 새 스프레드시트 생성, 이름은 아무거나 (예: `운동일지 DB`).
2. 탭은 만들 필요 없음. 스크립트가 `sessions`, `routines`, `exercises`, `meta` 탭을 자동 생성.

## 2. Apps Script 넣기
1. 시트 메뉴 **확장 프로그램 → Apps Script**.
2. 기본 `Code.gs` 내용을 지우고 이 폴더의 `apps-script/Code.gs` 전체를 붙여넣기.
3. 맨 위 `const TOKEN = 'CHANGE-ME';` 를 아무 문자열로 바꾸기 (예: `TOKEN = 'wl-2026-abc'`). 앱 설정에 같은 값을 넣게 됨.
4. 저장(Ctrl+S).

## 3. 웹앱으로 배포
1. 오른쪽 위 **배포 → 새 배포**.
2. 유형 선택(톱니) → **웹 앱**.
3. 설정:
   - 설명: 아무거나
   - 다음 사용자 인증 정보로 실행: **나**
   - 액세스 권한: **모든 사용자**  ← 중요 (앱이 로그인 없이 호출)
4. **배포** → 권한 승인 화면에서 계정 선택 → "확인되지 않은 앱" 경고가 나오면 *고급 → 이동* → 허용.
5. 나오는 **웹 앱 URL**(`https://script.google.com/macros/s/.../exec`) 복사.

## 4. 앱에 연결
1. 폰(또는 PC)에서 운동일지 → **설정** 탭 → **구글시트 동기화**.
2. 웹앱 URL과 토큰 입력 → **연결 테스트**. "연결 성공"이 뜨면 끝.
3. 이미 폰에 기록이 있으면 **지금 올리기**를 한 번 눌러 시트에 채우기.
4. 자동 동기화가 켜져 있으면 이후 저장할 때마다 시트에 반영되고, 앱을 열 때 시트가 더 최신이면 자동으로 가져옴.

## 동작 방식
- **올리기(push)**: 앱의 전체 데이터(운동 종목·루틴·세션)를 시트에 통째로 덮어씀.
- **가져오기(pull)**: 시트 내용으로 앱 데이터를 통째로 교체.
- 앱 열 때: 시트의 `updatedAt`과 앱의 `updatedAt`을 비교해 더 최신 쪽으로 맞춤.
- 오프라인이면 그냥 폰에만 저장되고, 다음 저장 때 올라감.
- 시트에서 직접 값을 고쳐도 되지만(`entries`·`items` 열은 JSON), 고친 뒤 `meta` 탭의 `updatedAt`을 현재 시각(ms)으로 올려야 앱이 가져감. 웬만하면 앱에서 수정하는 게 안전.

## 코드를 고쳤을 때
Apps Script 코드를 수정하면 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포** 를 해야 URL에 반영됨. URL은 그대로 유지됨.
